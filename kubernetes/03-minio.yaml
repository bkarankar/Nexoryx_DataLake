apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minio
  namespace: nexoryx-datalake
spec:
  serviceName: minio
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
    spec:
      containers:
      - name: minio
        image: minio/minio
        args:
        - server
        - /data
        env:
        - name: MINIO_ROOT_USER
          valueFrom:
            secretKeyRef:
              name: datalake-secrets
              key: MINIO_ROOT_USER
        - name: MINIO_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: datalake-secrets
              key: MINIO_ROOT_PASSWORD
        ports:
        - containerPort: 9000
        volumeMounts:
        - mountPath: /data
          name: minio-storage
  volumeClaimTemplates:
  - metadata:
      name: minio-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 100Gi
---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: nexoryx-datalake
spec:
  selector:
    app: minio
  ports:
  - port: 9000
